# URL of the REST API the dashboard connects to.
# Defaults to http://localhost:3001 when unset (local dev).
# Copy this file to .env.local and set the value for production or staging.
VITE_API_URL=https://your-api-host.example.com
